SELECT *
FROM EMPLOYEE;
SELECT *
FROM EMPLOYEE
WHERE EmpAge = 26;
SELECT *
FROM EMPLOYEE
WHERE EmpAge > 26;
SELECT *
FROM EMPLOYEE
WHERE EmpAge <> 35;

-- <> will give less and greater but not same..
-- Operators that can be used are '=','>','<','>=','<=',<>,BETWEEN,LIKE,IN...